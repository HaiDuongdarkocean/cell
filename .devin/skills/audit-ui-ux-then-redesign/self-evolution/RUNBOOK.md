# RUNBOOK

> ⚠️ **Superseded note (2026-09-08):** Tài liệu này tham chiếu Liquid Glass / glass material / blur — vật liệu này đã bị loại khỏi Cell. Luật thiết kế hiện hành: `docs/design-system/DESIGN_RATIONALE.md`; checklist: `docs/design-system/DESIGN.md`. Nội dung yêu cầu tính năng trong tài liệu vẫn hiệu lực; chỉ các mô tả visual kiểu glass/blur không còn áp dụng.

| Date | Page | User said | Turned out to be | Dial | Best finding | What was missed | Outcome |
|---|---|---|---|---|---|---|---|
| 2026-05-30 | Foundation showcase | border quá nhiều, _section_ không có gap, content bị che, thiếu ghi chú | main block chưa flex, section margin 0, card-in-card 3 lớp, thiếu caption/token note | DESIGN_VARIANCE 3, MOTION 2, DENSITY 7 | `.section` margin-bottom 0; `.main` display block | Cần xem thêm mobile scroll restore | Brief P0-P2 đã xác định |
| 2026-05-30 | Cross-page showcase audit | kiểm tra các trang dưới tree | Nhiều component mất border do token sai (input, button outline, select); nhiều showcase dùng inline style hardcoded; một số page render ra ngoài preview hoặc crash | DESIGN_VARIANCE 3, MOTION 3, DENSITY 6 | `--input-border` và `--button-outline-border` bị misused/missing; inline style object nhiều | Cần audit toàn bộ 100+ showcase | Cross-page brief đã tổng hợp |
| 2026-05-30 | Button / Input / Select / Tree fix | Button/Input không có màu, layout lộn xộn, thiếu chú thích; Tree mép trái không đều | `tokens.json` thiếu toàn bộ component color tokens (Button, Input); Tree leaf không render leading placeholder; showcase dùng inline style; Foundation `.main` không có gap | DESIGN_VARIANCE 3, MOTION 1, DENSITY 5 | `var(--button-*)` và `var(--input-*)` cần được grep sớm để phát hiện token mismatch trước khi audit chi tiết; CSS module + caption là pattern khắc phục lộn xộn | Phải thêm token, không chỉ sửa CSS module | Đã thêm token, sửa Tree, refactor showcase, build pass |
| 2026-05-30 | ShowcaseGallery preview card | Nội dung card trong showcase bị hai border đè nhau, border thứ ba là nội dung, không có padding, rất xấu | `ShowcaseGallery` có 3 lớp border lồng nhau: `previewOuter` (glass border), `previewInner` (solid border), `.stage` (dashed border); padding `previewOuter` chỉ 4px | DESIGN_VARIANCE 2, MOTION 1, DENSITY 5 | Bọc preview cần tối đa 1 border + 1 vùng nền phân cách; `previewOuter` chỉ cần glass shadow, `previewInner` giữ 1 border, `.stage` dùng nền `surface-elevated` + padding thay vì border | Cần kiểm tra gallery preview ngay trong audit ban đầu | Đã bỏ outer border, bỏ stage dashed border, tăng outer padding, stage có nền + padding |
| 2026-05-30 | ShowcaseGallery preview card — second pass | Người dùng vẫn thấy hai border sau khi sửa: `previewOuter` shadow + `previewInner` border | Khi một lớp có shadow và lớp kia có border, người dùng vẫn đọc là "hai border" | DESIGN_VARIANCE 1, MOTION 1, DENSITY 5 | Shadow cũng bị đọc thành border ở gallery card; cần một hoặc zero đường viền mạch lạc | Nên trả lời bằng visual, không chỉ computed `border` | Đã bỏ cả outer shadow lẫn inner border; chỉ còn glass background + stage background |
| 2026-05-30 | ShowcaseGallery preview card — final | Người dùng muốn đúng một Card bao phủ nội dung, không muốn nhiều card lồng; chê agent làm giải pháp khó khăn | `Card` component bị hỏng (`--card-bg`, `--card-fg`, `--card-shadow` không tồn tại, `border` viết double-solid), nên showcase đã "bù" bằng glass outer + custom inner + Box stage | DESIGN_VARIANCE 1, MOTION 1, DENSITY 5 | Sửa một component `Card` và dùng nó trực tiếp đơn giản hơn nhiều so với xếp wrapper tùy chỉnh | Phải kiểm tra component dùng sẵn trước khi chế wrapper; viewport padding làm card nhỏ hơn sidebar | Sửa `Card.module.css`, xóa `previewOuter`/previewInner, dùng 1 `Card` + `Box` stage trong suốt; viewport flex column, previewCard `flex: 1` + `align-self: center` để lấp đầy chiều cao |
| 2026-08-26 | Showcase dark mode — redesign audit | Showcase dark mode bị chê "xấu đau đớn, phối màu không đẹp"; người dùng chỉ vào `liquid-glass-warmth.html` làm tham chiếu | Dark mode dùng nền flat, glass highlight quá mờ, CTA neutral mất điểm nhấn; màu glass quá cool so với preset warmth | DESIGN_VARIANCE 6, MOTION 3, DENSITY 5 | CTA nên là primary fill + dark text như `liquid-glass-warmth`; glass cần inner highlight mạnh hơn; nền cần gradient + blob để tạo chiều sâu | Cần xác nhận xem primary fill CTA có vi phạm quy tắc "primary chỉ làm điểm nhấn" không — giải quyết bằng giới hạn 1 CTA chính mỗi view | Viết brief, tạo mockup `showcase-liquid-glass-v2.html` với gradient nền, glass highlight, CTA primary, glass secondary |
| 2026-05-30 | ShowcaseGallery preview card — redesign audit | Người dùng muốn redesign layout preview card với góc nhìn IA/UX/UI chuyên nghiệp | Preview card cần box contract, typography hierarchy, stage canvas; nhưng Heading/Text tokens bị missing nên tất cả chữ hiện 14px | DESIGN_VARIANCE 5, MOTION 1, DENSITY 4 | Hệ thống typography token bị hở: Heading dùng `--text-heading-3-font-size`, Text dùng `--text-body-font-size` — cả hai đều không tồn tại | Cần sửa root typography token, không chỉ CSS cục bộ | Đã thêm `text-*` tokens, sửa `color-text-secondary` self-reference, refactor `ShowcaseGallery` header/body/stage, build pass + verify 1280/534 |
| 2026-09-04 | StreamFlix mock page | audit những gì đang bị hỏng và thiếu sót ở streamflix để sửa giao diện | Duplicate subtitles (track + overlay cùng bật), player buttons không có handler, thiếu focus-visible, video không có poster, emoji star, label 10px, menu mobile thiếu a11y | DESIGN_VARIANCE 4, MOTION 1, DENSITY 4 | `subMode === 'track'` khiến `<track>` native và `activeCue` overlay hiện cùng lúc; chuyển overlay về `hash` mode + half-open cue | Cần verify scroll mobile/light mode toàn trang; vẫn còn P2 dùng custom header/card thay vì shared `Header`/`Card` | Đã sửa P0-P1, thêm `star` vào `ICON_CATALOG`, build + eslint pass, verify 1280/390/light/dark, commit 6 files |
| 2026-09-04 | Global Button component | khôi phục tất cả button liquid thành button solid bình thường, chừa subtitle panel và subtitle manager | Default `Button`/`IconButton` là `material="liquid"`, nên mọi button trong app đều glass; muốn solid default | DESIGN_VARIANCE 3, MOTION 1, DENSITY 3 | `Button.tsx` default `material = 'solid'` là 1 dòng; chỗ nào muốn glass phải truyền `material="liquid"` explicit | `variant="glass"` không có `.glass` CSS, phụ thuộc hoàn toàn vào `liquidRegular/Clear/Prominent`; nếu default solid mà quên `material="liquid"` thì glass bị mất style | Đổi default, thêm `material="liquid"` cho 9 files `features/subtitle/ui`, cập nhật `Button.showcase.tsx` + `Button.test.tsx`, build + eslint + `Button.test.tsx` pass, commit 12 files |


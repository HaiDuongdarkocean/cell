/**
 * Subtitle overlay types — barrel re-export from entities/subtitle
 * (Strangler Fig).
 *
 * Existing `@/types/subtitle` imports continue to work. New code SHOULD
 * import from `@/entities/subtitle`.
 */
export * from '@/entities/subtitle/types';

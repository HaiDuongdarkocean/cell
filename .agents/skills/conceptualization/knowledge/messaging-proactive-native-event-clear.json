{
  "id": "messaging-proactive-native-event-clear",
  "title": "Proactive clear on native event > cross-context round-trip",
  "category": ["messaging", "async", "state"],
  "tags": ["cross-context", "service-worker", "mv3", "native-event", "spa-navigation", "round-trip", "stale-state", "fallback"],
  "trigger": "Cross-context message chain (content → MAIN → SW → content) dùng để clear consumer state, nhưng bất kỳ hop nào fail silently để stale state visible",
  "principle": "A cross-context round-trip (content → MAIN → content → SW → content) is a request, not a guarantee. Each hop has its own failure mode (poll timeout, fetch error, SW eviction, message drop) and every failure is a silent skip — no error, no retry, no log. When the consequence of a missed signal is stale state visible to the user, the consumer MUST also listen for the native event that triggered the round-trip (`yt-navigate-finish`, `popstate`, `visibilitychange`) and clear its own state locally. The round-trip becomes the refill path (load new data), not the clear path. Clear is cheap, local, synchronous on the native event; refill is expensive, async, cross-context — they should not share a dependency chain.",
  "cases": [
    {
      "context": "YouTube SPA nav subtitle overlay persisted with video #1's cues on video #2 (no subtitles) — clear path depended on full background round-trip that silently dropped",
      "bad": "// Clear only runs inside AUTO_LOAD_SUBTITLES message handler\n// Chain: MAIN detect() → post → content relay → SW clearTab + send null → content clearCues\n// Failure modes (all silent skips):\n//  1. pollForVideoIdChange times out (2s) → detect() never runs\n//  2. InnerTube fetch error → tracks=[] posted late or not at all\n//  3. SW evicted mid-restart → DETECTED_SUBTITLES message dropped\n//  4. content-script has no proactive clear → overlay holds previous video's cues indefinitely",
      "good": "// Proactive: register native SPA-nav listeners, clear locally on URL change\nconst onSpaNav = (): void => {\n  if (lastAutoLoadUrl === undefined || lastAutoLoadUrl === location.href) return;\n  blockController?.clearCues();\n  offsetController?.loadCues(false);\n  latestTargetCues = [];\n  translatePrefill?.clear();\n  translatePrefill = null;\n  lastAutoLoadKey = undefined;\n  lastAutoLoadUrl = undefined;\n};\nwindow.addEventListener('yt-navigate-finish', onSpaNav);\nwindow.addEventListener('popstate', onSpaNav);\n// + matching removeEventListener in cleanup\n// Round-trip becomes refill path (load new subtitles), not clear path"
    }
  ],
  "applyFor": [
    "Cross-context state sync where the consumer shows stale state (overlay, panel, badge) — consumer needs local clear on the native event, not just a message from the producer",
    "MV3 service-worker message chains (SW eviction mid-round-trip drops messages silently) — consumer-side native event listener as fallback",
    "SPA frameworks with native nav events (`yt-navigate-finish`, `popstate`, framework-specific route-change events) — listen directly, don't rely on a re-broadcast through background",
    "Any \"clear on X\" path that currently only runs inside a message handler — verify the message always arrives; if not, add a local listener for X"
  ]
}

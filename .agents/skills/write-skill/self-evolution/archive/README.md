# Archive

This directory stores previous versions of `write-skill/SKILL.md`.
Each file is named `SKILL.<timestamp>.md` and is a backup before an autonomous self-correction update.

The current version always lives at `../SKILL.md`.

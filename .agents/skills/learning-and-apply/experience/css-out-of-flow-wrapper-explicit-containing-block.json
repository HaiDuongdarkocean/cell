{
  "id": "css-out-of-flow-wrapper-explicit-containing-block",
  "title": "Out-of-flow element moved into wrapper needs explicit containing block + 100% fill",
  "category": ["css", "rendering"],
  "tags": ["position-absolute", "containing-block", "wrapper-collapse", "out-of-flow", "width-100", "height-100", "position-relative"],
  "trigger": "Khi di chuyển element position:absolute/fixed vào wrapper mới và wrapper collapse height 0",
  "principle": "Khi di chuyển element out-of-flow (position: absolute/fixed) vào wrapper mới, wrapper KHÔNG kế thừa size của element — absolute elements contribute no height to containing block. Wrapper phải có explicit width:100%; height:100% để match original box. Container gốc phải thành positioned containing block (position: relative) để absolute wrapper fill đúng box, không phải ancestor rộng hơn. Apply cho cả initial setup VÀ restore path — reset wrapper về static = collapse lại.",
  "cases": [
    {
      "context": "Video absolute (art-player default) move vào wrapper không có width/height → wrapper collapse height 0 → video invisible trên page load",
      "bad": "// F0 position: static → outerWrapper absolute fill ancestor rộng hơn, overflow F0\n// videoWrapper width:auto, height:auto → video absolute contribute no height → collapse 0\nF0 (static)\n  └── outerWrapper (absolute, no size)\n      └── videoWrapper (static, auto)\n          └── video (absolute) // rect = 0×0 invisible",
      "good": "// 1. F0 thành positioned containing block\nif (getComputedStyle(f0).position === 'static') f0.style.position = 'relative';\n// 2. Wrapper explicit 100% để match original box\nouterWrapper.style.cssText = 'position:absolute;width:100%;height:100%;top:0;left:0';\nvideoWrapper.style.width = '100%';\nvideoWrapper.style.height = '100%';\n// video rect = 756×425 = F0 ✓"
    },
    {
      "context": "Restore path reset wrapper về static → collapse lại vì video vẫn absolute",
      "bad": "// hidePanelDocked: reset outerWrapper về static\nouterWrapper.style.position = 'static'; // video absolute → wrapper height 0 again → invisible",
      "good": "// Out-of-flow video: keep wrapper absolute 100% trên cả restore path\nif (isOutOfFlowVideo(video)) {\n  outerWrapper.style.position = 'absolute';\n  outerWrapper.style.width = '100%';\n  outerWrapper.style.height = '100%';\n} // don't reset to static — collapses height back to 0"
    }
  ],
  "applyFor": [
    "Content script DOM restructuring (move element into new wrapper)",
    "Art-player / video.js / any player với absolute-positioned video",
    "Drag-and-drop containers (element removed from flow → wrapper collapses)",
    "Portal/modal patterns (element moved to body → original container collapses)"
  ]
}
